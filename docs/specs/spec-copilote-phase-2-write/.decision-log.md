# Decision Log — spec-copilote-phase-2-write

## 2026-06-19 — Création (bmad-spec, distill depuis brainstorm 2026-06-19 + Phase 1 spec + deferred-work)

**Cadrage.** Incrément 1 de la Phase 2 du copilote : ouvrir l'écriture par le premier write-tool `seedContacts`, en posant simultanément la frontière R/W (Sécu #2) comme patron des futurs write-tools. Scope volontairement réduit à UN write-tool, sans danger (donnée de test, taguée, réversible). 2 capabilities (seed + annulabilité), 8 constraints, 7 non-goals.

**Écarts code vs brainstorm (vérifiés à la source, non inventés) :**
- Le brainstorm parle d'un flag `isTestData` ; le schéma `contacts` n'a PAS cette colonne mais a `source` (`["manuel","rapide","import_csv"]`). → Recommandation actée : taguer via une valeur `source` dédiée (`"seed"`) plutôt qu'une nouvelle colonne. Capturé en Assumption + Open Question.
- `@faker-js/faker` non installé → choix dépendance vs générateur inline laissé au dev (Assumption).
- `contactsRepository.create` existe, idempotent par `dedupKey` ; soft-delete (`archivedAt`) déjà en place → CAP-2 (annulabilité) s'appuie dessus, pas de nouveau mécanisme.

**Verdict self-validate (2 passes).**
*Pass 1 — Cohérence (Spec Law 1–6, 8) :* les 2 capabilities ont intent + success testable ; les success sont concrets (rows en BDD, isolement cross-tenant prouvé par test, prédicat de tag isolant). Constraints toutes décisionnelles (orchestration repo, closure userId, frontière R/W interne-only, réversibilité soft-delete, tag non ambigu, cap serveur, zod, parité sécu). 7 non-goals explicites (bornent fort : pas de resetTestData, pas de vraie donnée, pas d'UI, pas d'externe, pas de rewind journal, pas de mémoire, pas de durcissement historique). IDs CAP-1/CAP-2 neufs, stables. Prose lean. PASS.
*Pass 2 — Préservation :* claims load-bearing du brainstorm pour cet incrément (Archi #1 wrapper sur métier, Sécu #2 frontière R/W, Sécu #3 scope tenant, Sécu #6 cap, soft-delete réversible, indicateur « crée 10 contacts ») → tous landés en constraints/capabilities/success. Dette `deferred-work.md` (historique `assistant` falsifiable) → surfacée en Open Question avec position par défaut (différé, borné). Thèmes B/C/D non couverts (web/OAuth, UI popup, mémoire, import vrac, rewind) → explicitement renvoyés en non-goals (incréments futurs), pas droppés silencieusement. *Wrapper-only content* : aucun (le brainstorm est dense, peu de cérémonie). PASS.

**Open questions à trancher avant dev :** (1) mécanisme de tag (`source="seed"` recommandé vs colonne `isTestData`) ; (2) durcir l'historique `assistant` maintenant ou différer.

## 2026-06-19 — Open questions tranchées (Monsieur)

- **(1) Tag = `source = "seed"`** (option recommandée). Promu d'Assumption → Constraint : valeur `"seed"` ajoutée à `SOURCES` + zod frontière ; nettoyage = `where source='seed'`. Open Question retirée.
- **(2) Durcir l'historique MAINTENANT.** Promu en **CAP-3** : la route ne passe au modèle que des tours validés (ne garder que `user`, ou valider/borner strictement `assistant`/`tool`). Ferme la dette Phase 1 (`deferred-work.md`) avant l'ouverture de l'écriture. Constraint « historique client non digne de confiance » ajoutée ; non-goal correspondant retiré. Section Open Questions supprimée (vide).

Spec passe à **3 capabilities**, 9 constraints, 6 non-goals. Source `deferred-work.md` mise à jour (item historique sorti de la file différée → tracké par cette spec).

## 2026-06-19 — Implémentation (bmad-quick-dev) + code-review

**Livré (code).**
- CAP-1/CAP-2 — `seedContacts` (write-tool) dans `plume/src/lib/agent/tools.server.ts` : logique pure bouclant sur le vrai `contactsRepository.create` (Archi #1), tag `source="seed"`, `userId` clos par closure, clamp serveur `MAX_SEED=25` (SÉCU #6), zod à la frontière du tool, erreurs absorbées (posture `queryContacts`). `"seed"` ajouté à `SOURCES` (`domain/enums.ts`).
- CAP-3 — `selectTrustedTurns` (`run.server.ts`) : ne garde que les tours `user` ; appliqué à la route (`route.ts`, 400 si vide) + défense-en-profondeur dans `runAgentChat`. Ferme la dette Phase 1.
- Tests (`tests/agent/tools.test.ts`) : isolement cross-tenant, tag, clamp, ré-exécution honnête, prédicat de tag + réversibilité soft-delete (CAP-2), écartement des faux tours `assistant` (CAP-3). **Suite complète : 357 verts.** tsc 0, eslint 0 (barrières server-only / SDK / drizzle respectées).

**Code-review (high, 8 angles).** Correctness + conventions/spec : aucune anomalie. Triage cleanup (6 candidats) :
- **Corrigé** — `created` mensonger sur seed répété : le générateur déterministe initial faisait fusionner (dédup) tout 2ᵉ appel → `created:N` sans rien créer. Bascule sur un générateur **aléatoire** par défaut (e-mail à jeton unique ⇒ clé de dédup unique ⇒ jamais de fusion), conforme au Success signal « au hasard » et à l'Assumption « seeds aléatoires ». Générateur resté injectable → tests déterministes. Test de régression ajouté.
- **Rejetés (spec-mandatés)** — réutiliser `bulkCreate` (il fige `source="rapide"`, ne peut pas taguer `"seed"` ; la spec impose la boucle `create`) ; CAP-3 « bandaid » (le choix `user`-only est explicitement acté en constraint + decision-log, dette future documentée) ; deux bornes `count` (le `max(10_000)` zod doit rester large pour **clamper** au lieu de **rejeter** — « clampé, pas honoré »). Commentaire de clarification ajouté sur ce dernier.

**Reste pour le Checkpoint (humain).** Validation curl : `pnpm dev` puis POST `{"message":"crée 10 contacts au hasard"}` avec cookie de session → 10 contacts tagués `seed` en BDD, scopés, distinguables, supprimables ; vérifier qu'aucune clé API n'apparaît côté réseau client.
