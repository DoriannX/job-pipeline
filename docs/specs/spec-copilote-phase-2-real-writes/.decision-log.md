# Decision log — spec-copilote-phase-2-real-writes

## 2026-06-20 — Create (inc.3, cible « Thème B complet » tranchée par Monsieur)

**Cadrage.** Deux candidats inc.3 (resetTestData vs write-tools vraie donnée) posés à Monsieur → choix **Thème B complet** : `createContact`, `composeMessage`, import vrac. `resetTestData` poussé en non-goal.

**Décisions tranchées (contractuelles).**
- **Envoi externe = HORS scope, tranché** (pas une open question) : `project-context.md` interdit tout auto-send au MVP (« l'humain envoie ») + Sécu #4. `composeMessage` ne persiste qu'un brouillon ; l'envoi reste l'action humaine du parcours existant.
- **`composeMessage` réutilise le pipeline Composeur existant** (voix few-shot + `generateMessage` + `sanitize()` déterministe, canal-aware), pas un prompt neuf — le moat s'applique à l'identique. Factorisation en fonction serveur partagée demandée.
- **Import = contacts only** ; drafting par contact = agent chaînant `composeMessage` (composabilité), pas de mega-tool — borné pour le coût.
- **Sync NON re-spécifiée** : CAP-4 vérifie l'héritage du pont d'invalidation inc.2 (les tools rejoignent `WRITE_TOOL_NAMES`, zéro nouveau code de sync).

**Gaps remontés en Assumptions (vérifiés dans le code, pas inventés).** `messagesRepository` n'a pas de méthode d'insertion de brouillon (seul `markSent`→`envoye`) → `createDraft` probable. `Message` n'a pas d'`archivedAt` → réversibilité du brouillon via cascade d'archivage du contact (option simple) ou retrait repo minimal. Pipeline voix inline dans `/api/composer/route.ts` → à extraire. Canal par défaut + valeur `source` import → à confirmer dev.

**Self-validate — Pass 1 (cohérence, Spec Law 1–6, 8).** 4 capabilities, chacune intent+success testable (CAP-1 provenance/dédup/scope ; CAP-2 sanitize + aucun `envoye` ; CAP-3 dédup/scope/plafond ; CAP-4 revue diff = zéro sync nouvelle). Constraints bendent toutes une décision (orchestration repo, réutilisation pipeline, no-send, provenance vraie, réversibilité, bornes zod, sync héritée, parité sécu). Non-goals explicites (≥7). Success signal démontrable (3 actions observables + sync). IDs frais CAP-1..4, stables. Prose lean.

**Self-validate — Pass 2 (préservation).** Claims source couverts : brainstorm Thème B (Capacité #3 advisor→doer, #4 import vrac, composeur IA), Stratégie #1/#3 (outil de test = assistant produit), Sécu #2 (frontière R/W), #3 (scope tenant), #4 (human-in-loop send → no-send), #6 (bornes) ; `project-context.md` (Composeur source de vérité, `sanitize()` à l'import, no auto-send, design-system) ; `deferred-work` sync générique → CAP-4. Aucun drop load-bearing. Ceremony wrapper : néant de substance perdu.

**Verdict.** Spec cohérente et préservée. Aucune open question bloquante (envoi externe tranché hors scope). Quatre assumptions purement implémentation, sans incidence sur le contrat.
