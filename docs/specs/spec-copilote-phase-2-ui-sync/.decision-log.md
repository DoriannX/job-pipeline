# Decision log — spec-copilote-phase-2-ui-sync

## 2026-06-19 — Création (incrément 2 de la piste copilote)

Distillé depuis : commande `/bmad-spec` (intent fournie), `deferred-work.md` (items « sync UI
temps réel des mutations agent » + « erreur in-band côté client »), brainstorm 2026-06-19
(Thème C / UX #1), et lecture du code (`reseau/page.tsx`, `ReseauClient.tsx`, route + wrapper
agent, `(app)/layout.tsx`).

3 capacités : CAP-1 UI copilote (icône flottante + popup, 1re surface), CAP-2 sync temps réel
générique des mutations, CAP-3 erreur in-band (fermeture dette Phase 1). 1 companion
spec-authored (`sync-mechanism-options.md`), 2 companions adoptés (specs inc.1 phase 1 et 2).

Décision de cadrage : l'item deferred-work « erreur in-band » est INTÉGRÉ (CAP-3) plutôt que
laissé en non-goal — l'arrivée de l'UI rend un flux tronqué silencieux visuellement
indistinguable d'un succès, donc load-bearing maintenant. Le passage à
`toUIMessageStreamResponse` (requis pour CAP-3) mutualise avec le signal de sync de la piste A.

Mécanisme de sync : laissé en décision de design (Spec Law rule 2 — WHAT pas HOW), mais
contraint fort (générique/transverse, vérité serveur, front bête) ce qui écarte de facto les
pistes B (events) et C (store client). Recommandation = piste A (invalidation/`router.refresh`)
documentée en companion + assumption. Vérifié que `node_modules/next/dist/docs/` n'est pas
bundlé dans cette version → analyse fondée sur le pattern repo existant.

### Self-validate — verdict

**Pass 1 (coherence).** Les 3 capacités ont intent + success testable (CAP-1 : icône sur 3
onglets + stream ; CAP-2 : 10 contacts apparaissent sans reload + mécanisme à point unique ;
CAP-3 : token d'erreur terminal sur échec mid-stream). Constraints bendent toutes une décision
(générique vs patch ; vérité serveur vs store client ; format de réponse ; parité sécu ;
design-system). ≥1 non-goal, explicites (5). Success signal concret et démontrable. IDs stables
(nouveau spec, CAP-1..3 frais). Prose lean. RAS à corriger.

**Pass 2 (preservation).** Toutes les sources load-bearing landées :
- deferred-work « sync UI temps réel » → CAP-2 + companion (3 pistes préservées) ✔
- deferred-work « erreur in-band » → CAP-3 + constraint `toUIMessageStreamResponse` ✔
- brainstorm UX #1 (icône flottante → popup, présent partout, PC/mobile) → CAP-1 + assumption
  point de montage `(app)/layout.tsx` ✔
- contraintes de parité (server-only, barrières ESLint, auth 401, wrapper, userId closure,
  frontière R/W, selectTrustedTurns) → Constraints ✔
- exigence « mécanisme générique le plus haut niveau » → Constraint #1 + non-goal « pas de sync
  par page » ✔
- design-system (project-context) → Constraint #7 + open question niveau de finition ✔

Contenu wrapper-only (process BMad, traçabilité de la piste) non porté dans le kernel : normal.
Aucune perte load-bearing silencieuse.

Ouvertures restantes (open_questions) : choix mécanisme, déclencheur de refresh, niveau de
finition UI. Toutes nécessitent un arbitrage humain/design avant ou pendant bmad-quick-dev.

## 2026-06-19 — Open questions tranchées par Monsieur (AskUserQuestion)

- **Mécanisme de sync = piste A (pont d'invalidation).** `router.refresh()` réutilisant le
  pattern repo, source de vérité serveur. B (events métier client) et C (store client)
  écartés. → désormais contractuel (Constraints), plus une simple reco.
- **Déclencheur = un seul `router.refresh()` en fin de stream, conditionné à « ≥1 write-tool
  appelé »** (détecté serveur-side). Pas de refresh par token. → Constraints.
- **Finition UI = pleinement design-system d'emblée** (pas de MVP « stylé assez » à polir).
  Fraunces/Quicksand, offsets durs, mauve=action, mascotte, dès cet incrément. → Constraints.

Effet sur le kernel : section Open Questions supprimée (zéro résiduel) ; Assumptions « mécanisme
recommandé à confirmer » remplacée par décisions contractuelles ; companion `sync-mechanism-
options.md` marqué DÉCIDÉ (piste A) en conservant la trace des alternatives. Sources annotées :
les 2 items de `deferred-work.md` marqués SPEC'D / IN-FLIGHT vers cette spec.
</content>
