# Secrets — ne JAMAIS committer (repo PUBLIC)
.env

# Données générées au runtime
data/
output/

# Python — environnements & caches (régénérés via `pip install -r requirements.txt`)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Worktrees git internes — non committables (checkouts liés)
.claude/worktrees/

# NB : _bmad/, docs/ et .claude/ (skills dont story-loop, settings) SONT versionnés
# pour qu'un clone soit identique. Retire-les de ce fichier = arrête de les suivre.
